// 'use script' no arquivo js serve para acusar alguns erros que ele ignora

x += y; // x = x + y
x -= y; // x = x - y
x *= y; // x = x * y
x /= y; // x = x / y
x %= y; // x = x % y (resto de divisão)
x **= y; // x = x ** y (exponenciação)

// OPERADOR TERNÁRIO
var idade = 19;
var podeBeber = (idade >= 18) ? 'Pode beber' : 'Não pode beber';
// condição ? true : false
/* Geralmente é usado quando é necessário atribuir um valor a uma variável
   dependendo de uma condição*/
